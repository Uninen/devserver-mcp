# test configuration
servers:
  backend:
    command: "uv run fastapi dev backend.py"
    working_dir: "./testapp"
    port: 8000

  frontend:
    command: "pnpm dev"
    working_dir: "./testapp/frontend"
    port: 3000
